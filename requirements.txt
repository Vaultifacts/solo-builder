# Solo Builder dependencies
# Core CLI (solo_builder_cli.py) uses stdlib only.

# REST API
flask>=3.1

# Anthropic SDK (direct API calls — no subprocess needed)
anthropic>=0.40

# PDF snapshots (optional — gracefully disabled if missing)
matplotlib>=3.10
numpy>=2.2

# Telegram bot (optional — only needed for telegram_bot/bot.py)
python-telegram-bot>=20.0
