# HANDOFF TO ARCHITECT (from RESEARCH)

## Context
- Active task: `TASK-013`
- Scope: orchestrator workflow tooling only. No product code changes.
- Goal constraint: add one authoritative `claude/NEXT_ACTION.md` generated by the orchestrator.

## Evidence collected
- `tools/claude_orchestrate.ps1` already writes `claude/NEXT_ACTION.md` (lines 180–227) using an inline here-string template with `{{PLACEHOLDER}}` variable substitution.
- `claude/NEXT_ACTION.md` exists and is correctly rendered for the current task/phase (TASK-013 / triage / RESEARCH).
- `claude/AGENT_ENTRY.md` already lists `claude/NEXT_ACTION.md` as required read #4.
- All role prompt blocks inside `claude_orchestrate.ps1` already include `claude/NEXT_ACTION.md` in RequiredReads arrays.
- `$nextActionTemplatePath = Join-Path $repoRoot 'claude/templates/NEXT_ACTION_TEMPLATE.md'` is set at line 8 of the orchestrator, and `Test-Path $nextActionTemplatePath` is checked at line 212 — but `claude/templates/` directory does not exist and `NEXT_ACTION_TEMPLATE.md` has never been created, so the orchestrator silently uses the inline fallback.
- `claude/prompts/` directory does not exist; no external prompt files reference `NEXT_ACTION.md` (the prompt text lives inside `claude_orchestrate.ps1` in role contract definitions).
- `pwsh tools/research_extract.ps1` confirmed the above by scanning `claude/` and `tools/`.

## Observations
- Stable/confirmed:
  - Core TASK-013 acceptance criteria (orchestrator writes NEXT_ACTION.md, file contains correct fields, STATE.json stays authoritative, no product code changes) are already satisfied by the existing orchestrator implementation.
  - The only concrete artifact referenced but absent is `claude/templates/NEXT_ACTION_TEMPLATE.md`.
- Uncertain:
  - Whether TASK-013 requires the external template file as a deliverable, or whether the inline fallback is sufficient. Given the orchestrator explicitly references the path, creating the file formalizes the design intent.

## Hypotheses (ranked)
- H1: The only missing deliverable is `claude/templates/NEXT_ACTION_TEMPLATE.md`. Creating it externalizes the inline template, making the template customizable without editing the orchestrator script.
- H2: No other workflow files need changes — AGENT_ENTRY.md, CONTROL.md, RULES.md already reference or are consistent with NEXT_ACTION.md.

## Constraints / Non-negotiables
- No product-code changes under `solo_builder/*`.
- No changes to `tools/claude_orchestrate.ps1` or state-machine semantics.
- No changes to AGENT_ENTRY.md, CONTROL.md, RULES.md (already correct).
- Do not create `claude/prompts/` files (not required by acceptance criteria).
- Preserve deterministic workflow conventions.
